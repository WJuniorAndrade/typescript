{ /* Arquivos com as configurações do compilador Ts */
    "compilerOptions": {

        "outDir": "dist/js",  /* A propriedade indica para onde os arquivos compilados serão gerados e salvos.*/

        "target": "ES6",/* A propriedade "target" indica para o compilator tsc (TypeScript Compiler) para qual versão do Javascript o código escrito em TypeScript deve ser compilado. Isso significa que o resultado final será arquivos Javascript sem qualquer referência para a sintaxe do Typescript. */

        
        "noEmitOnError": true,  /* significa que ele não vai gerar um arquivo "js" enquanto seu TypeScript não estiver passando na compilação, isso evita que você gere um arquivo "js" com problema */

        "noImplicitAny": true
},
    "include": [
        "app/**/*" /* Alternativa errada! A propriedade indica para o compilador onde estão os arquivos ".ts" que ele deve compilar. */
    ]
}